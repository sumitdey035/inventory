%tr.pointer
  %td= brand.name
  %td= brand.owner_name
  %td.last
    = link_to edit_admin_brand_path(brand), remote: true do
      .fa.fa-edit.fa-lg
    = link_to [:admin, brand], data: {confirm: 'Are you sure?'}, method: :delete do
      .fa.fa-trash.fa-lg