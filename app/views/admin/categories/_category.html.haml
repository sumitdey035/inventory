%tr.pointer
  %td= category.name
  %td= category.categorized_object.classify
  %td.last
    = link_to edit_admin_category_path(category), remote: true do
      .fa.fa-edit.fa-lg
    = link_to [:admin, category], data: {confirm: 'Are you sure?'}, method: :delete do
      .fa.fa-trash.fa-lg